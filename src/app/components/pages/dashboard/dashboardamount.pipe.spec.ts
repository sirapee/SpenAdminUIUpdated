import { DashboardamountPipe } from './dashboardamount.pipe';

describe('DashboardamountPipe', () => {
  it('create an instance', () => {
    const pipe = new DashboardamountPipe();
    expect(pipe).toBeTruthy();
  });
});
