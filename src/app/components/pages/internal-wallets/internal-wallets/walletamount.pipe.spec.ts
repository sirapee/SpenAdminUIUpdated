import { WalletamountPipe } from './walletamount.pipe';

describe('WalletamountPipe', () => {
  it('create an instance', () => {
    const pipe = new WalletamountPipe();
    expect(pipe).toBeTruthy();
  });
});
